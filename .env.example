# Copy to .env and fill in (never commit .env — it is gitignored).
# Used by python/config.py via python-dotenv when notebooks/scripts import config.

OPENAI_API_KEY=
# Optional OpenAI-compatible base URL (leave blank for default OpenAI API)
# OPENAI_API_BASE=
